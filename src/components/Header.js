import React from "react";

const Header = ({ state, handleClick }) => {
  return (
    <div className="bg-foreground-blue max-w-lg w-full rounded-full text-white-cream text-6xl font-bold p-10 mt-10">
      <h1>Race Date</h1>
    </div>
  );
};

export default Header;
